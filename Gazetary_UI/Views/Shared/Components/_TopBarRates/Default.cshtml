@model Dtos.ExchangeRateDto

<span class="flex items-center gap-1">
    <span class="hidden md:inline">USD:</span> @Model.USD
</span>
<span class="flex items-center gap-1">
    <span class="hidden md:inline">EUR:</span> @Model.EUR
</span>